# .github/workflows/main.yml
name: DEV Run Tests on Push Main Event

on:
  push:
    branches: [main]

jobs:
  run-tests:
    uses: ./.github/workflows/_template-tests.yml
    with:
      env-name: dev